---
description: Ingest material into the knowledge vault right now - the files or topic given, or everything in sources/ not ingested yet
---

Apply the `vault-ingest` skill right now to: $ARGUMENTS

If nothing is given above, find every file in the sources folder that has no summary page in the vault yet (check `index.md` and `summaries/`) and ingest them all. When you're done, summarize which pages you created and updated, and anything you couldn't read.
